"""Bundled fixture data for `dd report generate --sample`.

The :mod:`dd_cli.reporting.fixtures.sample` module exposes ``sample_*``
functions matching the shape of DefectDojo's API responses. They let
users preview the report layout end-to-end without configuring a real
DefectDojo URL or API token.
"""
